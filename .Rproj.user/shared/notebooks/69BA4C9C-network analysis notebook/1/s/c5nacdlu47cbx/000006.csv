"0","# The number of nodes and links is the grain for the two data frames; hence aggregation is needed if rows are not unique"
"0","# nrow(nodes); length(unique(nodes$id))"
"0","# nrow(links); nrow(unique(links[,c(""from"", ""to"")]))"
